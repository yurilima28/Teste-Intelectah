@using Agendamento.Models;


@model List<ConsultaModel>


@{
    ViewData["Title"] = "Cadastro de consulta";

}

<div class="text-center">

    <div class="d-grid gap-2 d-md-flex justify-content-md-start">
        <a class="btn btn-primary" role="button" asp-controller="Consulta" asp-action="Criar">Adicionar no consulta</a>
    </div>
    <br />

    @if (TempData["MensagemSucesso"] != null)
    {
        <div class="alert alert-success" role="alert">
            <button type="button" class="btn btn-danger btn-sm close-alert" arial="Close">X</button>
            @TempData["MensagemSucesso"]
        </div>

    }
    @if (TempData["MensagemErro"] != null)
    {
        <div class="alert alert-danger" role="alert">

            <button type="button" class="btn btn-danger btn-sm close-alert" arial="Close">X</button>
            @TempData["MensagemErro"]
        </div>

    }
    <br />

    <div class="text-center">
        <h1 class="display-4">Consulta</h1>

    </div>


    <form asp-controller="Consulta" asp-action="Criar" method="post">

        <ul class="list" id="Exame">
            <h1>Lista Consultas</h1>

        </ul>
        @if (Model != null && Model.Any())
        {
            <table class="table" id="table-Consulta">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Nome</th>
                        <th scope="col">Observaçãoes</th>
                        <th scope="col"></th>
                    </tr>
                </thead>
                <tbody>

                    @if (Model != null && Model.Any())
                    {
                        foreach (ConsultaModel consulta in Model)
                        {

                            <tr>
                                <th scope="row">@consulta.Id</th>
                                <td>@consulta.</td>
                                <td>@consulta.</td>
                                <td>
                                    <div class="btn-group" role="group">
                                        <a role="button" class="btn btn-primary display flex" asp-route-id="@consulta.Id" asp-controller="Consulta" asp-action="Editar">Editar</a>
                                        <a role="button" class="btn btn-danger" asp-route-id="@consulta.Id" asp-controller="Consulta" asp-action="ApagarConfirmacao">Apagar</a>

                                    </div>
                                </td>
                            </tr>
                        }



                    }
                </tbody>
            </table>

        }






    </form>
